# Dynamic atmosphere and camera-transform proof

These 1280×900 rasters were generated by `Tools/SceneDirectoryProof/SceneDirectoryProof.cpp`. The harness records the production `WorkspacePanel`, `EditorPanel`, `SceneDirectoryPanel`, and camera property controls through the real recording surface.

- `editor-sun-props.png` shows the authored Sun component and its expanded six-field runtime controls in the actual editor layout. The properties footer has no History destination.
- `editor-camera-properties.png` shows the Editor Camera-only Properties / Bookmarks inspector. Its Transform card now reflects the camera component's live pose (`Position Y = 2`, `Rotation X = 100`, `Rotation Y = 15`) rather than a disconnected all-zero entity transform.

The harness retains the CPU atmosphere reference solely for deterministic raster proof. `EditorHost` no longer invokes that path: its normal viewport uses `ViewportSkySurface`'s device-local RGBA16F compute surface.
